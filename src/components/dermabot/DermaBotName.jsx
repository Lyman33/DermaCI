export default function DermaBotName({ className = '' }) {
  return (
    <span className={className}>
      <span style={{ color: '#0d2818' }}>Derma</span>
      <span style={{ color: '#00A878' }}>Bot</span>
    </span>
  );
}